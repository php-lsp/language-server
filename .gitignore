.idea/
vendor/
var/

# Environment overrides (local secrets, dev settings)
# .env is committed with safe defaults; use .env.local for overrides
.env.local
.env.*.local
tools/
composer.phar
composer.lock
.phpunit.result.cache
.DS_Store
Thumbs.db
.claude/worktrees/
/test.php
